CC     = gcc
CFLAGS = -Wall -g

all: user1 user2

user1: user1.c ksocket.h libksocket.a
	$(CC) $(CFLAGS) user1.c -o user1 -L. -lksocket -lpthread

user2: user2.c ksocket.h libksocket.a
	$(CC) $(CFLAGS) user2.c -o user2 -L. -lksocket -lpthread

clean:
	rm -f user1 user2
